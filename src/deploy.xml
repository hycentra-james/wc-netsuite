<?xml version="1.0" encoding="UTF-8"?>
<deploy>
    <!-- WC-682: scoped to ONLY this change - the script fix + its new custom record.
         Does NOT re-import the existing faq/custitem/fedex objects; those stay untouched
         in NetSuite. This intentionally narrows the deploy.xml back to a per-change scope
         instead of the broader faq-wide <objects> block used previously. -->
    <files>
        <path>~/FileCabinet/SuiteScripts/Hycentra/Case/CaseAttachmentGDriveSync_SS.js</path>
    </files>
    <objects>
        <path>~/Objects/gdrive/customrecord_hyc_gdrive_folder_cache.xml</path>
    </objects>
</deploy>
